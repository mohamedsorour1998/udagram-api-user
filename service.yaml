apiVersion: v1
kind: Service
metadata:
  labels:
    app: udagram-api-user
  name: udagram-api-user
spec:
  ports:
  - name: "8100"
    port: 8100
    targetPort: 8100
  selector:
    app: udagram-api-user